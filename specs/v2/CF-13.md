# CF-13 — PWA Shell

## What this is

Progressive Web App configuration: the web app manifest and service worker.

## Output files

- `app/manifest.json`
- `app/sw.ts`

## `app/manifest.json`

```json
{
  "name": "Hoboken Parking",
  "short_name": "HP Parking",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#ffffff",
  "theme_color": "#ffffff",
  "icons": [
    {
      "src": "icon-192.png",
      "sizes": "192x192",
      "type": "image/png"
    },
    {
      "src": "icon-512.png",
      "sizes": "512x512",
      "type": "image/png"
    }
  ]
}
```

## `app/sw.ts`

```typescript
const CACHE_NAME = "hoboken-parking-v2";

const APP_SHELL_URLS = [
  "/",
  "/index.html",
  "/app.js",
  "/style.css",
  "/manifest.json",
  "https://unpkg.com/leaflet@1.9.4/dist/leaflet.js",
  "https://unpkg.com/leaflet@1.9.4/dist/leaflet.css",
];
```

### Strategy

- **Install:** cache all `APP_SHELL_URLS` and call `skipWaiting()`.
- **Activate:** delete all caches with names not equal to `CACHE_NAME`.
- **Fetch:**
  - For `data/latest.json`: network-first (try network, fall back to cache on failure).
  - For any URL in `APP_SHELL_URLS` (or matching the app origin): cache-first (return from cache if available, otherwise fetch and cache).
  - All other requests: pass through to network.

### Compilation

`app/sw.ts` is compiled by esbuild as part of the build script:

```
esbuild app/sw.ts --bundle --outfile=app/sw.js --platform=browser
```

This step is already included in the `build` script in `package.json` (CF-00).

## Hard constraints

- No `as any`. No `!`. Named exports are not needed (service workers have no exports — the file uses `self.addEventListener`).
- `CACHE_NAME` must be `"hoboken-parking-v2"` exactly.
- `skipWaiting()` must be called on install.
- `data/latest.json` must use network-first strategy.
- `app/sw.ts` is compiled to `app/sw.js` by esbuild — do NOT write `app/sw.js` directly; write the TypeScript source.
- Tests verify only static file contents (string/JSON checks via `fs.readFileSync`); do not attempt to import or execute the service worker in Node.

## Tests

Test file: `tests/unit/pwa.test.ts`

### manifest.json contents

- GIVEN `app/manifest.json` is read as JSON, WHEN the `name` field is accessed, THEN it equals `"Hoboken Parking"`.
- GIVEN `app/manifest.json` is read as JSON, WHEN the `short_name` field is accessed, THEN it equals `"HP Parking"`.
- GIVEN `app/manifest.json` is read as JSON, WHEN the `display` field is accessed, THEN it equals `"standalone"`.
- GIVEN `app/manifest.json` is read as JSON, WHEN the `icons` array is accessed, THEN it contains an entry with `"src": "icon-192.png"` and `"sizes": "192x192"`.
- GIVEN `app/manifest.json` is read as JSON, WHEN the `icons` array is accessed, THEN it contains an entry with `"src": "icon-512.png"` and `"sizes": "512x512"`.
- GIVEN `app/manifest.json` is read as JSON, WHEN the `start_url` field is accessed, THEN it equals `"/"`.

### sw.ts static content

- GIVEN `app/sw.ts` is read as a string, WHEN searched for the cache name constant, THEN it contains the literal string `"hoboken-parking-v2"`.
- GIVEN `app/sw.ts` is read as a string, WHEN searched for `skipWaiting`, THEN it contains `skipWaiting()`.
- GIVEN `app/sw.ts` is read as a string, WHEN searched for the network-first data URL, THEN it contains `"data/latest.json"`.
- GIVEN `app/sw.ts` is read as a string, WHEN the APP_SHELL_URLS list is examined, THEN it contains `"/index.html"` and `"/app.js"` and `"/style.css"`.
