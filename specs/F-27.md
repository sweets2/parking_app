## F-27 Get Current Location Button `[TODO]`

Adds a crosshair button to the map UI that snaps the blue browsing-mode dot to the user's real GPS position and centers the map there. After pressing it, normal map-tap behavior resumes seamlessly — `renderPositionMarker` is called by both paths, so GPS and tap-to-place are interchangeable.

Three files are modified, no new files:

- `app/index.html` — add `#locate-control` with `#locate-btn`
- `app/style.css` — style the button, shift `#tow-legend` down to make room
- `app/main.ts` — wire the button in `initBrowserApp()`

---

### F-27.1 HTML (`app/index.html`)

Add `#locate-control` immediately before `#tow-legend`:

```html
<div id="locate-control">
  <button id="locate-btn" type="button" title="Get my current location" aria-label="Get my current location">
    <svg width="18" height="18" viewBox="0 0 18 18" fill="none" xmlns="http://www.w3.org/2000/svg">
      <circle cx="9" cy="9" r="5.5" stroke="currentColor" stroke-width="1.8"/>
      <line x1="9" y1="1" x2="9" y2="5" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/>
      <line x1="9" y1="13" x2="9" y2="17" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/>
      <line x1="1" y1="9" x2="5" y2="9" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/>
      <line x1="13" y1="9" x2="17" y2="9" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/>
    </svg>
  </button>
</div>
```

**Tests**
- GIVEN `app/index.html` source, THEN it contains an element with `id="locate-control"`.
- GIVEN `app/index.html` source, THEN it contains a button with `id="locate-btn"`.
- GIVEN `app/index.html` source, THEN `#locate-btn` has `type="button"`.
- GIVEN `app/index.html` source, THEN `#locate-btn` has `aria-label="Get my current location"`.
- GIVEN `app/index.html` source, THEN `#locate-control` appears before `#tow-legend` in the DOM.

---

### F-27.2 CSS (`app/style.css`)

Add `#locate-control` at `top: 84px, left: 10px` (the slot currently occupied by `#tow-legend`). Button styling mirrors Leaflet's zoom control aesthetic. Also update `#tow-legend` from `top: 84px` to `top: 126px` (84 + 34px button height + 8px gap).

```css
/* ─── Get Current Location button ─────────────────────────── */
#locate-control {
  position: fixed;
  top: 84px;
  left: 10px;
  z-index: 1000;
}

#locate-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 34px;
  height: 34px;
  padding: 0;
  background: rgba(255, 255, 255, 0.96);
  border: 1.5px solid #d1d5db;
  border-radius: 6px;
  cursor: pointer;
  box-shadow: 0 1px 5px rgba(0, 0, 0, 0.25);
  color: #374151;
}

#locate-btn:active {
  background: #f3f4f6;
}

#locate-btn:disabled {
  opacity: 0.5;
  cursor: default;
}
```

Update `#tow-legend`:
```css
#tow-legend {
  top: 126px;   /* was 84px; locate-btn (34px) + 8px gap */
}
```

**Tests**
- GIVEN `app/style.css` source, THEN it contains `#locate-control` with `position: fixed`.
- GIVEN `app/style.css` source, THEN it contains `top: 84px` for `#locate-control`.
- GIVEN `app/style.css` source, THEN it contains `left: 10px` for `#locate-control`.
- GIVEN `app/style.css` source, THEN `#locate-btn` has `width: 34px` and `height: 34px`.
- GIVEN `app/style.css` source, THEN `#tow-legend` has `top: 126px` (not `top: 84px`).

---

### F-27.3 Button wiring (`app/main.ts`)

In `initBrowserApp()`, add the following block after the tow-toggle wiring and before the clear-button wiring. No new imports are needed — `renderPositionMarker`, `centerOnSpot`, and `SavedSpot` are already imported.

```typescript
// Wire "Get Current Location" button
const locateBtn = document.getElementById("locate-btn");
if (locateBtn !== null) {
  locateBtn.addEventListener("click", () => {
    if (!("geolocation" in navigator)) return;
    locateBtn.setAttribute("disabled", "");
    navigator.geolocation.getCurrentPosition(
      (pos) => {
        locateBtn.removeAttribute("disabled");
        renderPositionMarker(pos.coords.latitude, pos.coords.longitude);
        centerOnSpot({ lat: pos.coords.latitude, lng: pos.coords.longitude, savedAt: new Date().toISOString(), address: null });
      },
      () => {
        locateBtn.removeAttribute("disabled");
      },
      { enableHighAccuracy: true, timeout: 10000 }
    );
  });
}
```

`centerOnSpot` is used solely for its `_map.panTo` side-effect; the other `SavedSpot` fields are unused by its implementation. One-shot `getCurrentPosition` (not `watchPosition`) is intentional — after the GPS snap, map taps naturally override the dot position.

**Tests**
- GIVEN a DOM with `#locate-btn` and `navigator.geolocation` available, WHEN the button is clicked, THEN `disabled` is set on the button during the geolocation call.
- GIVEN geolocation succeeds, THEN `renderPositionMarker` is called with the returned coordinates.
- GIVEN geolocation succeeds, THEN `centerOnSpot` is called with `lat` and `lng` matching the returned coordinates.
- GIVEN geolocation succeeds, THEN `disabled` is removed from the button.
- GIVEN geolocation fails (error callback fires), THEN `disabled` is removed from the button.
- GIVEN `navigator.geolocation` is not defined, WHEN the button is clicked, THEN no error is thrown.
